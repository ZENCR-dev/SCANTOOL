# 扫码页用同码闩区分「连刷」与「隔码再扫」

真机连续对焦同一面单会多次解码；若每次都打 API，台账第二次必为已收过，屏幕会在新已收成功后立刻警报。决定：在客户端增加 **同码闩**——新已收成功后闩住该码，同码连刷只维持绿色新已收展示且不重复请求 API；解码到其它非空内容后解除闩，再扫回旧码才显示已收过。台账写入规则不变。

## Consequences

- 必须区分「扫码结果」（台账）与「扫码展示反馈」（UI）
- 同码闩逻辑应可单测；与时间冷却正交
- 首次写表延迟仍受 Apps Script 影响；连刷卡顿应明显下降
