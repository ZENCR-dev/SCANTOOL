# 收货扫码核对

一人用 iPhone 扫快递面单，对照一批应收单号确认是否到齐；Google 表格是本批收货的唯一台账。

## Language

**收货表**：
本批收货的唯一台账，即一张 Google 表格（只用其第一个工作表）。
_Avoid_: 数据库, 主数据, 库存表

**挂载**：
把扫码工具指向某一张收货表的动作（通过粘贴表格链接或 ID）。
_Avoid_: 绑定, 连接数据库, 导入项目

**应收单号**：
收货表中列为应收、需要在本批扫到的快递单号。
_Avoid_: 订单号, tracking（作口语时可说「单号」，文档与代码用本词）

**命中**：
扫到的内容在去掉首尾空白后，与某一应收单号完全一致。
_Avoid_: 模糊匹配, 包含匹配, 相似

**已收**：
某应收单号已被成功记入收货表为收到。
_Avoid_: 完成, 签收（除非业务另有签收流程）

**未收**：
某应收单号尚未记为已收；在收货表中表现为状态为空或字面量「未收」。
_Avoid_: 缺失, 待扫描（口语可说「还没扫到」）

**已收过**：
在已经隔开其它码之后，再次扫到本批中已是已收的单号时，向收货人发出的警告反馈；不改变收货表。
_Avoid_: 重复成功, 二次确认, 连刷同码时的误报（连刷同码应维持新已收反馈，见「同码闩」）

**不在清单**：
扫到的内容未命中任何应收单号时的结果；不记为已收。
_Avoid_: 错收, 异常单（本产品不以错收为主防目标）

**扫码结果**：
一次对收货表生效的判定：新确认为已收、已收过、或不在清单；写表失败则本次不成立为已收。
_Avoid_: 扫码事件（未判定前）, 回调, 仅 UI 回放的展示态

**扫码展示反馈**：
扫码页上给收货人看的提示（可与当次 API 扫码结果不同）：连续对准同一码时，在同码闩解除前可反复展示「新已收」绿色，而不反复报「已收过」。
_Avoid_: 扫码结果（台账判定）

**同码闩**：
扫码会话中记录的「刚成功新已收的那个码」；摄像头连续读出同一码时保持闩、维持新已收展示且不重复打「已收过」；读到其它非空码内容后闩解除。
_Avoid_: 冷却（冷却管时间间隔；同码闩管「是否换码」）

**扫码时间**：
某应收单号被记为已收时，写入收货表的确认时刻（表格原生日期时间）。
_Avoid_: 日志时间, 上传时间

**批次**：
一次收货作业对应的预置项，含可读名称与一张收货表；扫码页用下拉选择后挂载。
_Avoid_: 项目, 环境, 目录表（指 Google 里的批次总表）

**扫码主屏**：
挂载成功后的界面：隐藏选表区，以扫码展示反馈与取景框为主，顶栏显示表文件名与进度。
_Avoid_: 挂载页, 设置页

**本地清单**：
挂载后缓存在手机上的本批应收单号及状态副本，用于立刻判定扫码展示反馈。
_Avoid_: 离线数据库, 缓存服务器
